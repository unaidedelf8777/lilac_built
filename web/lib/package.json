{
  "name": "lilac-client-library",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest",
    "lint": "prettier --plugin-search-dir ../../ --check . && eslint .",
    "format": "prettier --plugin-search-dir ../../ --write .",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "openapi-typescript-codegen": "^0.24.0",
    "typescript": "^5.0.0"
  }
}

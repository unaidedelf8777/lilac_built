{
  "name": "lilac-client-library",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest",
    "lint": "prettier --plugin-search-dir ../../ --check . && eslint ."
  }
}
